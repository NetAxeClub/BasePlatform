# 四任务总功能验收清单（2026-03-16）

> 用途：`Gate 4` 总体验收时逐项勾选。未全部满足前，不得给出最终 `PASS`。

## A. 测试底座

- [ ] 4 个 worktree 均有统一可用的 `config/config.json`
- [ ] 4 个 worktree 均有统一可用的本地插件路径
- [ ] 4 个 worktree 可在各自 `backend/` 目录下运行目标 `manage.py test`
- [ ] 不再出现 `apps.monitor` 或插件自动加载链导致的测试启动失败

## B. 任务 1 基线

- [ ] 任务 1 合同文档已冻结
- [ ] 标准集合命名规则已冻结
- [ ] 公共字段 9 项已冻结
- [ ] 任务 1 合同测试通过
- [ ] 任务 2 已吸收任务 1 基线
- [ ] 任务 3 已吸收任务 1 基线
- [ ] 任务 4 已吸收任务 1 基线或已完成契约对齐确认

## C. 任务 1 功能

- [ ] `device_api` 成为新增采集能力唯一入口
- [ ] 未在 `apps.automation` 增加采集扩展
- [ ] 主矩阵厂商已按计划补齐
- [ ] `arp` 标准化统一写入 `plan_arp`
- [ ] `mac`、`lldp`、`aggre_port`、`ip_interface`、`interface_brief` 按统一链路入库
- [ ] `/base_platform/device_api/collection-results/latest/` 可看到标准化结果

## D. 任务 2 功能

- [ ] `plan_collect_device_main` 触发策略正确
- [ ] `execute_all_collections` 触发策略正确
- [ ] `execute_sub_plan(use_local=true)` 触发策略正确
- [ ] `record_plan_data` 触发策略正确
- [ ] `record_collection` 触发策略正确
- [ ] 接口数据未齐时不会提前触发分析
- [ ] 产生的 `AnalysisRun` 状态正确
- [ ] `/base_platform/network_analysis/interface-utilization/overview/` 结果正确
- [ ] `/base_platform/int_utilization/interfaceused/` 与同批次快照一致

## E. 任务 3 功能

- [ ] 地址定位只使用同一 `execute_time` 批次数据
- [ ] 多候选 `MAC` 选择逻辑正确
- [ ] 聚合口展开逻辑正确
- [ ] `LLDP` 邻居闭环定位正确
- [ ] 无 `MAC` 时正确标记 `unresolved`
- [ ] `located / partial / unresolved` 语义稳定
- [ ] `/base_platform/workflow_center/address-location/` 与 `network_analysis` 语义一致

## F. 任务 4 功能

- [ ] NetClaw-CN 稳定接口清单完整
- [ ] 主入口与 `v1` 别名存在性已断言
- [ ] `device_api` 稳定接口存在
- [ ] `workflow_center` 稳定接口存在
- [ ] `network_analysis` 稳定接口存在
- [ ] 禁止依赖项清晰
- [ ] 需审批项清晰
- [ ] 可直接实施项清晰
- [ ] 未让 NetClaw 直接依赖 legacy 模型或原始 Mongo 表

## G. 仓库边界与审批

- [ ] 未经批准的 `asset` 模型改造为零
- [ ] 未经批准的破坏性兼容改造为零
- [ ] 所有需审批改造都已单列为提案
- [ ] 没有把未审批事项混入“已完成”

## H. 证据与发布准备

- [ ] 四个任务都提交了单任务验收记录
- [ ] 每个任务都附带执行命令
- [ ] 每个任务都附带结果摘要
- [ ] 每个任务都附带关键接口返回样例
- [ ] 每个任务都说明了剩余风险
- [ ] 四任务集成验收记录完整

## I. 最终结论

- [ ] 总负责人给出 `PASS / CONDITIONAL_PASS / FAIL`
- [ ] 风险级别已标注
- [ ] 未决事项已列清
- [ ] 放行建议已列清

# device_api 开发归档（2026-03-16）

## 1. 目的

本文件用于在不依赖当前会话上下文的情况下，快速恢复 `device_api` 当前开发进度、工作区状态和下一步动作。

建议下次进入工作区时，优先阅读：

1. 本文档
2. `backend/apps/device_api/doc/README.md`
3. `backend/apps/device_api/doc/实施方案与TODO.md`

## 2. 当前工作区状态

- 当前分支：`codex/device-api-replace-automation-2026-03-15`
- 当前状态：`IN_PROGRESS`
- 当前工作区包含未提交改动
- 当前新增但未提交的 migration：`backend/apps/device_api/migrations/0009_summary_plan_sync_config.py`

本轮已完成的主要改动分组：

- 父方案级配置简化
  - 增加 `enabled_collection_types`
  - 增加 `collection_method`
  - 父方案保存时自动同步子方案启用状态
- 方案级配置聚合能力
  - 新增父方案 `field-mappings` 聚合/回写接口
  - 新增父方案 `validate` 一键验证接口
- NETCONF 处理器补齐
  - H3C：补齐 `mac/ip_interface/lldp/aggre_port`
  - Huawei：补齐 `arp/mac/ip_interface/lldp/aggre_port`

## 3. 本轮已落地能力

### 3.1 父方案主导配置

已落地：

- `DeviceCollectionPlans` 新增方案级采集类型和采集方式字段
- 父方案 `create/update/sync_collect_plans` 会自动：
  - 创建缺失子方案
  - 关闭未选中的采集类型
  - 同步 `netmiko_enabled/netconf_enabled`

相关文件：

- `backend/apps/device_api/models.py`
- `backend/apps/device_api/services_new.py`
- `backend/apps/device_api/serializers.py`
- `backend/apps/device_api/views.py`
- `backend/apps/device_api/migrations/0009_summary_plan_sync_config.py`

### 3.2 方案级字段映射聚合

已落地接口：

- `GET /base_platform/device_api/collection-plans/{id}/field-mappings/`
- `PATCH /base_platform/device_api/collection-plans/{id}/field-mappings/`

用途：

- 以 `collection_type` 聚合展示各子方案的 `*_path` 和 `*_field_mappings`
- 支持按父方案维度批量回写

### 3.3 方案级一键验证

已落地接口：

- `POST /base_platform/device_api/collection-plans/{id}/validate/`

用途：

- 对父方案下所有已启用子方案做一次聚合验证
- 支持：
  - `use_local=true` 本机直连
  - 传 `south_driver` 走南向驱动
- 返回结果按 `collection_type` 分组

### 3.4 NETCONF 处理器补齐

已补齐 H3C：

- `arp/netconf`
- `mac/netconf`
- `ip_interface/netconf`
- `lldp/netconf`
- `aggre_port/netconf`
- `mac_evpn/netconf`

已补齐 Huawei：

- `arp/netconf`
- `mac/netconf`
- `ip_interface/netconf`
- `lldp/netconf`
- `aggre_port/netconf`

说明：

- 解析逻辑优先复用了 `automation/tools/h3c.py` 和 `automation/tools/huawei.py` 的旧语义，降低行为漂移风险
- 当前 H3C/Huawei 的常见 NETCONF 结构已由 `apps.device_api.tests` 覆盖

## 4. 已执行验证

本轮关键验证已经执行并通过：

```bash
python3 -m py_compile backend/apps/device_api/models.py \
  backend/apps/device_api/services_new.py \
  backend/apps/device_api/serializers.py \
  backend/apps/device_api/views.py \
  backend/apps/device_api/platform_profiles.py \
  backend/apps/device_api/filters.py \
  backend/apps/device_api/processors/h3c.py \
  backend/apps/device_api/processors/huawei.py \
  backend/apps/device_api/tests.py \
  backend/apps/device_api/migrations/0009_summary_plan_sync_config.py

python3 backend/manage.py test apps.device_api.tests -v 2

python3 backend/manage.py makemigrations --check --dry-run device_api
```

说明：

- 本轮主要是 `device_api` 定向验证
- 还没有跑整仓 `python3 backend/manage.py test`

## 5. 当前剩余重点

### 5.1 优先级最高

- `P1-E`：继续收窄 `tools/` 的职责，明确仅做 Layer 2 规范化
- `P3-1`：补预处理/执行链路测试，减少后续协议扩展回归风险

### 5.2 次优先级

- `P3-2`：补 MongoDB 复合索引
- `P3-3`：继续补 Cisco / Ruijie / Hillstone 厂商覆盖

### 5.3 暂不优先

- Telemetry 真正执行逻辑
- 子方案协议配置拆表

## 6. 下次恢复建议

下次启动后建议按这个顺序继续：

1. 执行 `git status --short`，确认当前工作区仍包含本批改动
2. 阅读本文件和 `backend/apps/device_api/doc/README.md`
3. 若需要继续开发，先执行：

```bash
python3 backend/manage.py test apps.device_api.tests -v 2
```

4. 若要在本地联调父方案新能力，先执行：

```bash
python3 backend/manage.py migrate
```

5. 下一步优先从以下两条中选一条继续：
   - 清理 `tools/` 为纯 Layer 2 规范化器
   - 增强 `device_api` 执行链路测试

## 7. 风险与注意事项

- 当前改动仍未提交，属于工作区态
- `0009_summary_plan_sync_config.py` 尚未在数据库执行
- 目前验证以 `apps.device_api.tests` 为主，尚未覆盖全仓回归
- `tools/` 仍保留部分 legacy 兜底职责，后续若继续改处理链路，需要避免和 `processors/` 责任重叠

## 8. 关联文档

- `doc/13-device_api阶段性总结-2026-03-13.md`
- `doc/16-device_api替代automation架构审视与迁移计划.md`
- `backend/apps/device_api/doc/README.md`
- `backend/apps/device_api/doc/实施方案与TODO.md`

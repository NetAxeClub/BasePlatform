# device_api 遗留任务

## 1. 当前遗留项

### 1.1 P5 豁免收敛

当前仍需按 `P5-WAIVER-20260317` 收敛以下事项：

| ID | 任务 | 截止时间 | 状态 | 验证 |
|---|---|---|---|---|
| P5W-04 | `window` 口径分集合收敛：`ARP/MAC` 匹配率 >= 95%，`LLDP/interface*` 不再为 0 | 2026-04-17 18:00:00 | IN_PROGRESS | `p5_rollout_report_window_round2_fixed_sample.json`、`p5_rollout_report_strict_round2_fixed_sample.json`、`p5_window_convergence_snapshot.json` |
| P5W-05 | 形成豁免到期复核结论（通过 / 续期 / 回退）并更新发布说明 | 2026-04-30 18:00:00 | TODO | `p5_acceptance_decision.json`、`p5_release_notes.json` |

### 1.2 画像与绑定例外

- 13 台设备仍未命中当前画像
- 1 台设备缺 SSH 账号
- 这些设备不阻塞当前放行，但仍需清理例外清单

对应证据：

- [p1_coverage_report.json](./p1_coverage_report.json)

### 1.3 Telemetry 真执行

- 当前 Telemetry 仅有延期门禁
- 仍需补真实执行、标准化和回归测试

### 1.4 查询与性能

- 继续补采集结果查询性能验证
- 复核 Mongo 复合索引是否覆盖当前查询路径
- 继续收敛慢查询与无索引访问

### 1.5 分析链路联调

- 持续验证 `network_analysis` 对标准化结果的消费稳定性
- 保证接口利用率、地址定位不回退到 legacy 表语义

## 2. 跟进原则

后续进入 `device_api` 的改动，必须持续满足：

1. 不让能力回流到 `apps.automation`
2. 不破坏标准化结果合同
3. 功能变化同步补测试和必要说明

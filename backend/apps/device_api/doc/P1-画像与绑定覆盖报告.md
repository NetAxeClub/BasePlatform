# P1 画像与绑定覆盖报告

## 1. 结论

截至 2026-03-17，P1 通过验收。

当前状态：

- 内置画像按真实模板/处理器能力收敛，不再把长尾厂商声明为全量支持
- 默认父方案固定使用 CLI/Netmiko 作为兜底执行方式
- 默认方案、子方案、自动绑定和覆盖审计已形成可执行命令链
- 最新一次审计（2026-03-16）结果：`total=3898 ready=3884 blocked=14`
- 当前阻塞仅剩：`profile_not_matched=13`、`missing_ssh_account=1`
- `placeholder_textfsm_template` 阻塞已清零

## 2. 本次执行

执行命令：

```bash
backend/venv/bin/python backend/manage.py migrate device_api
backend/venv/bin/python backend/manage.py audit_device_api_coverage \
  --sync-default-plans \
  --auto-bind \
  --sample-limit 20 \
  --output backend/apps/device_api/doc/p1_coverage_report.json
```

执行结果：

- `device_api` 迁移已全部应用到 `0009_summary_plan_sync_config`
- 自动绑定结果：`created=0 updated=3885 skipped=13`
- 覆盖审计结果已写入 `backend/apps/device_api/doc/p1_coverage_report.json`

## 3. 画像与方案调整（当前基线）

本阶段新增或收敛的画像：

- 新增 `H3C-router-cli`
- 新增 `H3C-firewall-cli`
- 收紧 Huawei / H3C / Ruijie / Cisco / ZTE / Maipu / Mellanox / Centec 的 `supported_collection_types`

补充说明：

- Huawei / H3C 的 OSPF 与 ISIS CLI 子项已恢复到画像和默认模板映射
- 路由协议模板已升级为可解析模板，当前审计已无占位模板阻塞

## 4. 例外清单

### 4.1 未纳入当前画像范围的 13 台设备

这些设备已明确归入 P1 例外清单，不阻塞当前 P1 完成：

- `sangfor`：3 台
- 厂商为空：3 台
- `F5`：2 台
- `inspur`：2 台
- `nettap`：2 台
- `Citrix`：1 台
- 其余均已纳入当前画像覆盖范围

### 4.2 账号阻塞 1 台

- `10.254.3.190`
- `serial_num=219801A12T9213Q0011Q`
- `profile=H3C-modern-netconf`
- 阻塞原因：`missing_ssh_account`

处理建议：

- 若继续本地双栈采集，补 SSH 账号
- 若只走 NETCONF 或南向驱动，调整绑定执行方式并复核子方案启用状态

## 5. 对 P1 定义的对照

| ID | 结果 | 状态 |
|---|---|---|
| P1-01 | 已补目标厂商画像匹配规则，支持中文设备类型与 H3C 路由器/防火墙画像 | DONE |
| P1-02 | 已校准默认命令和模板，路由协议模板已从占位升级为可解析版本 | DONE |
| P1-03 | 默认父方案已按画像补齐 `enabled_collection_types`，`collection_method` 默认固定为 CLI/Netmiko | DONE |
| P1-04 | 默认父方案和子方案同步已纳入命令链 | DONE |
| P1-05 | `PlansToDevice` 自动绑定已执行并产生真实结果 | DONE |
| P1-06 | 已输出阻塞项 JSON 报告和书面例外清单（13 台画像外设备 + 1 台缺 SSH 账号） | DONE |
| P1-07 | 绑定执行方式已在审计中校验，缺失执行方式会直接报阻塞 | DONE |

## 6. 后续进入 P2 前的输入

P2 可直接基于以下输入继续推进：

- `backend/apps/device_api/doc/p1_coverage_report.json`
- 本文档中的例外清单
- 当前已收敛后的画像和默认 CLI 方案矩阵

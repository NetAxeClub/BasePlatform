# P1.5 CLI 画像与占位模板报告

## 1. 结论

截至 2026-03-16，P1.5 通过验收。

当前阻塞：

- 历史阻塞已清零：审计不再把占位模板视为能力就绪，且 Huawei 路由器画像已补齐

## 2. 本阶段补齐内容

### 2.1 路由协议 TextFSM 模板补齐

- `hp_comware_display_ospf_peer.textfsm`
- `hp_comware_display_ospf_interface.textfsm`
- `hp_comware_display_isis_peer_verbose.textfsm`
- `huawei_vrp_display_ospf_peer_brief.textfsm`
- `huawei_vrp_display_ospf_interface.textfsm`
- `huawei_vrp_display_isis_peer_verbose.textfsm`

这些模板已从“占位状态”升级为可解析模板，并通过 TextFSM 冒烟测试验证：

- CLI 画像、模板文件和索引保持闭环
- 模板具备表头/分隔行过滤与记录提取能力
- 字段级精修继续在 P3 推进

### 2.2 画像与默认模板调整

- 恢复 Huawei / H3C 画像中的 OSPF 与 ISIS CLI 子项
- 新增 Huawei 路由器画像 `Huawei-router-cli`，补齐 Huawei 交换机/路由器/防火墙三类 CLI 基线
- 将 Cisco `arp` 默认模板从空值改为 `cisco_ios_show_ip_arp.textfsm`
- 保持 H3C 路由器、防火墙 CLI 画像与当前仓库模板能力一致

## 3. 验证

代码级验证：

```bash
backend/venv/bin/python backend/manage.py test \
  apps.device_api.tests.DeviceApiProtocolExtensionTests \
  apps.device_api.tests.DeviceApiHealthExtensionTests -v 2
```

运行级验证：

```bash
backend/venv/bin/python backend/manage.py audit_device_api_coverage \
  --sync-default-plans \
  --auto-bind \
  --output backend/apps/device_api/doc/p1_coverage_report.json
```

验证结论：

- 新审计结果：`total=3898 ready=3884 blocked=14`
- 阻塞仅剩：`profile_not_matched=13`、`missing_ssh_account=1`
- `placeholder_textfsm_template` 阻塞命中为 0
- Huawei router 画像已落代码（`Huawei-router-cli`），当前资产集中暂无该画像命中设备

## 4. 对 P1.5 定义的对照

| ID | 结果 | 状态 |
|---|---|---|
| P1.5-01 | H3C 交换机 / 路由器 / 防火墙 CLI 画像已建立 | DONE |
| P1.5-02 | Huawei 交换机 / 路由器 / 防火墙 CLI 画像已补齐并完成实库复核 | DONE |
| P1.5-03 | Cisco / Ruijie / Hillstone / ZTE / Maipu / Mellanox / Centec 基础 CLI 画像已形成 | DONE |
| P1.5-04 | CLI 命令画像已回填到 `PROFILE_NETMIKO_SUB_PLAN_DEFAULTS` | DONE |
| P1.5-05 | 缺失模板已补齐并升级为可解析模板 | DONE |
| P1.5-06 | 模板名与 `index` 已对齐 | DONE |
| P1.5-07 | 审计口径已区分占位模板阻塞，实库复跑通过 | DONE |
| P1.5-08 | 已补最小回归测试 | DONE |
